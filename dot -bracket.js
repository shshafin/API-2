const info = {
  name: "shafin",
  age: 19,
};

// dot notation
console.log(info.name);

// bracket notation
console.log(info["age"]); // string property name

const name = "name"; //can call variable
console.log(info[name]);
